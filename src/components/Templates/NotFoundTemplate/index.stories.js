import React from 'react'
import {storiesOf} from '@storybook/react'
import NotFoundTemplate from './index'

storiesOf('Template/NotFoundTemplate', module).add('Default', () => (
  <NotFoundTemplate />
))
