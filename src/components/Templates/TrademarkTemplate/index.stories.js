import React from 'react'
import {storiesOf} from '@storybook/react'
import TrademarkTemplate from './index'

storiesOf('Template/TrademarkTemplate', module).add('Default', () => (
  <TrademarkTemplate />
))
