import React from 'react'
import {storiesOf} from '@storybook/react'
import ShareButtonList from './index'

storiesOf('Organisms/ShareButtonList', module).add('Default', () => (
  <ShareButtonList />
))
